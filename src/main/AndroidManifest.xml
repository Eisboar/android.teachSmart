<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wanda" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Wanda" >
        <activity
            android:name="com.wanda.ui.LoginActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.wanda.ui.QuestionSheetView"
            android:label="@string/title_activity_question_sheet" >
        </activity>
        <activity
            android:name="com.wanda.ui.SheetsView"
            android:label="@string/title_activity_question_sheets_view" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
